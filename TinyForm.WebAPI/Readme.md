# .Net9 + ASP.NET Core + EntityFrameworkCore.InMemory + Mvc.Abstractions + xUnit

this part of the project represents Web API with 3 endpoints for receiving and storing submissions without hard coded models. 
/Post
/GetAllAsync
/GetByIdAsync

Backend knows nothing about the data structure.
